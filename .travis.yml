language: php

php:
  - 5.5
  - 5.6
  - 7.0
  - 7.1

before_install:
  - composer self-update
  
install:
  - composer install --no-interaction --prefer-source
  
script:
  - ./vendor/bin/tester -s -c ./tests/php.ini ./tests